def main():
    x = int(input("What's x ? "))
    print("x squared is", square(x))


def square(n):
    return n * n


main()

# float will allow and add numbers that are decimels
# x = float(input("What's x ? "))
# y = float(input("What's y ? "))

# this from of the print function adds commas to numbers the surpass the hundreths length
# print(f"{z:,}")
